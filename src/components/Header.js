import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '10px', backgroundColor: '#333', color: '#fff', position: 'absolute', top: 0, left: 0, width: '100%' }}>
            <h1>Network Graph</h1>
        </header>
    );
};

export default Header;
